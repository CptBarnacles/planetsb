<% if line_item == @current_item %>
	<tr id="current_item">
<% else %>
	<tr>
<% end %>
  <td><%= line_item.sponsor.planet_id %></td>
  <td><%= line_item.duration %></td>
  <td><%= line_item.amount %></td>

  <td class="item_price"><%= number_to_currency(line_item.total_price) %></td>
</tr>